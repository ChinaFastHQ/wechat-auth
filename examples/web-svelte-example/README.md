# Svelte + Express example

Copy `.env.example` to `.env`, fill in the app IDs and secrets, and register
`http://localhost:5175/` with WeChat. Run the Svelte app on `5175` and Express on `4103` with:

```bash
pnpm --filter web-wechat-auth-svelte-example dev
```
