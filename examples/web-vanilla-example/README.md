# Vanilla JavaScript + Express example

Copy `.env.example` to `.env`, fill in the app IDs and secrets, and register
`http://localhost:5176/` with WeChat. Run the vanilla Vite app on `5176` and Express on `4104`:

```bash
pnpm --filter web-wechat-auth-vanilla-example dev
```
